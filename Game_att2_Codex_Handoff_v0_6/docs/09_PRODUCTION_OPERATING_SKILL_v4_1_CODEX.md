# Game att2 - Production Operating Skill v5.0 (Codex Edition)

## Status

This file preserves the handoff reading order and provides a human-readable entry point. The discoverable repository skill is:

```text
../../.agents/skills/game-att2-production/SKILL.md
```

Codex must use that skill for Game att2 requirement, design, simulator, test, evidence, playtest, gate, or production-audit work. The skill is a workflow control system, not authority to expand project scope.

## What v5.0 changes

- Converts the prose-only operating guide into a repository-scoped Codex skill with valid metadata.
- Front-loads trigger conditions so the skill activates for requirements, rules, code, tests, research, H1, and gate work.
- Routes audit, implementation, design-proposal, H1, and Encounter 3 paper tasks through separate approval paths.
- Treats the top-level Turkish PDF and archived reports as historical evidence when they conflict with current authority.
- Separates simulator evidence from player-experience evidence using mechanics -> dynamics -> experience claims.
- Adds an H1 timing/reflex accessibility gate and protects strategy from reflex dominance.
- Adds gated plugin routing without introducing a runtime dependency: GitHub handles remote repository workflow, Codex Security handles trust-boundary changes, and all other connectors remain conditional.
- Requires current factual metrics to come from fresh repository evidence rather than copied status text.

## Binding anchors

- Can owns identity-level, creative, product, and major technical decisions.
- Current runtime scope remains S-001 -> Jeff -> emergency graft -> Anna -> Grafting Table.
- Encounter 3 remains paper-only; Warden runtime and Unity remain blocked.
- Combat Rules v0.5 governs current simulator semantics.
- State-derived outcomes remain mandatory; actions mutate state and never select endings directly.
- The bounded H1 fidelity fixture is implemented. Its one-second terminal diagnostic
  is inadequate for human-facing evidence; the current gate is owner approval of the
  bounded visual interaction-lab plan.
- Passing tests proves implementation fidelity, not fun, accessibility, balance, or market demand.

## Compatibility rule

When an older prompt names this file as the production skill, read this entry point and then load the canonical `SKILL.md` above. Do not maintain two divergent copies of the workflow.
